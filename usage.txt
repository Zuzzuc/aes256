Usage is "(path to script)" inputfile outputfile task password", example of usage :
”${BASH_SOURCE[0]} input.txt encrypted.txt enc password”
Where ${BASH_SOURCE[0]} is the file path, input.txt is the input file, encrypted.txt is the output, enc is the mode(can be enc(encrypt) or dec(decrypt), and password is the passoword."